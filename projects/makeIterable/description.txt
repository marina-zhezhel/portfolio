Написать функцию makeIterable, которая принимает аргументом функцию, которая умеет обрабатывать данные некоторым образом, и возвращает функцию, которая умеет обрабатывать массивы данных. Если оригинальная функция что-то возвращала, то новая функция должна возвращать массив результатов

// Убирает пробельные символы с обоих концов строк
function trim(str) {
	//...
}
trim('    Hello \r\n'); // -> 'hello'

var iTrim = makeIterable(trim)
iTrim(['   Hello', ' how   ', ' are', 'you']); // -> ['Hello', 'how', 'are', 'you']